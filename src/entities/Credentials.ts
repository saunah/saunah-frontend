export type Credentials = {
    username: string
    password: string
    repeatPassword: string
    name: string
    firstname: string
    email: string
    telephone: string
    place: string
    street: string
    zip: string
}
