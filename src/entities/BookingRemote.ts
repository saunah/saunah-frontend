/**
 * This namespace represents the booking types
 * from the backend. It is a seperate file
 * as those types are huuuge.
 */
export namespace BookingRemote {
    export type Response = { michi: true }
    export type Request = { michi: true }
}
